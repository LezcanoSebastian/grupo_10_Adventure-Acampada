window.addEventListener("load", () => {
    let  emailRegistrado = qs(".emailRegistrado")
    const delay = 1000
    setTimeout(email, delay);
        function email(){
            emailRegistrado.style.display = "none"
        }
})

